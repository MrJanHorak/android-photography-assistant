package com.janhorak.shutterdeck.core

/** App-wide appearance modes. NIGHT is a red-on-black scheme that preserves night vision. */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
    NIGHT,
}
